# =============================================================
# Backend (FastAPI) — Python dependencies
# =============================================================
# Cài đặt: pip install -r requirements.txt
# Môi trường khuyến nghị: Python 3.11+
# =============================================================

# ── FastAPI & Server ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Database ──────────────────────────────────────────────────
sqlalchemy>=2.0.29
psycopg2-binary>=2.9.9
alembic==1.13.1

# ── Authentication ────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# ── RAG Pipeline — ML / AI ───────────────────────────────────
# NOTE: torch có thể nặng (~2GB). Nếu chỉ dùng API (gemini/openai),
#       vẫn cần torch vì LegalRetriever load bi-encoder.
torch>=2.1.0
sentence-transformers>=2.5.0
transformers>=4.40.0
accelerate>=0.27.0
faiss-cpu>=1.7.4           # Dùng faiss-gpu nếu có CUDA GPU
rank-bm25>=0.2.2           # BM25 hybrid search
numpy>=1.24.0
httpx==0.27.0

# ── LLM API Clients ───────────────────────────────────────────
openai>=1.10.0             # OpenAI + OpenRouter
google-generativeai>=0.7.0 # Google Gemini

# ── Redis (rate limiting) ─────────────────────────────────────
redis==5.0.4

# ── Validation & Config ───────────────────────────────────────
pydantic>=2.7.1
pydantic-settings>=2.2.1
python-dotenv==1.0.1

# ── Logging ───────────────────────────────────────────────────
loguru==0.7.2
